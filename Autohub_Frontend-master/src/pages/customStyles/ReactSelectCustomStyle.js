const customStyles = {
	control: (base) => ({
		...base,
		height: 30,
		minHeight: 30,
		size: 25,
		fontSize: 11,
		// menuSize: 10,
	}),
};
export const customStyles2 = {
	control: (base) => ({
		...base,
		// height: 30,
		// minHeight: 30,
		size: 25,
		fontSize: 11,
		// menuSize: 10,
	}),
};
export default { customStyles, customStyles2 };
